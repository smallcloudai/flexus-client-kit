# Scheduled Sync Integrations

This directory is reserved for polling and scheduled synchronization integrations.

Typical responsibilities:
- checkpoint and cursor storage
- idempotent upsert flow
- retry-safe periodic fetch and merge
