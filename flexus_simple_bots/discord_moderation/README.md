# Discord Moderation

Deletes invite links (optional), URL-regex matches, enforces per-channel regex rules, per-user rate limits, and optional new-account quarantine role or kick. Logs actions to a mod channel and stores events in MongoDB.

Separate bot token from Onboarding/FAQ/Engagement. Does not modify Karen or `fi_discord2`.
