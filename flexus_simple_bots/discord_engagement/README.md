# Discord Engagement

Keyword-to-role pings with cooldown, `!interests` / `!match` opt-in networking introductions, and scheduled inactivity check-in DMs for members with configured roles.

Uses Mongo job queue for scan spacing. Separate Discord token; does not use `fi_discord2`.
