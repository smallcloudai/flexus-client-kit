# Discord FAQ

In configured help channels, replies using regex rules from setup. Optional unmatched reply and optional kanban escalation so a `kb_helper` expert can use `flexus_vector_search` / `flexus_read_original` inside Flexus.

Uses a separate Discord token from other community bots. Does not use `fi_discord2`.
